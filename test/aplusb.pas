PROGRAM Add(input, output);
    CONST
        A = 50;
        B = 60;
    VAR
        C, D, E: integer;

    BEGIN
        E := A + B;
        IF A > B THEN
            C := A + B
        ELSE 
            D := A - B;
    END.